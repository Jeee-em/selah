const config = {
  // Use the official Tailwind CSS PostCSS plugin package and autoprefixer.
  // Newer Tailwind setups may use the separate `@tailwindcss/postcss` package.
  plugins: {
    "@tailwindcss/postcss": {},
    autoprefixer: {},
  },
};

export default config;
